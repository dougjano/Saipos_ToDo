import { MessageMaterialModule } from './message-material-module';

describe('MessageMaterialModule', () => {
  it('should create an instance', () => {
    expect(new MessageMaterialModule()).toBeTruthy();
  });
});
